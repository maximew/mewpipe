<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
	<div class="container-fluid">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<%= link_to 'Mewpipe', root_path, class: "navbar-brand" %>
		</div>
		<div class="navbar-collapse collapse in">
			<ul class="nav navbar-nav up">
				<li class="dropdown">
					<a href="#" class="dropdown-toggle glyphicon glyphicon-facetime-video" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> <%= t('Video') %><span class="caret"></span></a>
					<ul class="dropdown-menu">
						<li><%= link_to t('addvideo_html'), new_video_path %></li>
						<li><%= link_to t('mostview_html'), mostview_videos_path %></li>
						<li><%= link_to t('lessview_html'), lessview_videos_path %></li>
						<li role="separator" class="divider"></li>
						<li><%= link_to t('myvideos_html'), myvideos_videos_path %></li>
					</ul>
				</li>
			</ul>
			<ul class="nav navbar-nav navbar-right form-inline">
				<% if user_signed_in? %>
		 	        <li><%= link_to current_user.firstname.to_s + " " + current_user.lastname, edit_user_registration_path %></li>
	 				<li><%= link_to t('logout'), destroy_user_session_path, method: :delete %></li>
		            <!-- THIS CHANGES THE METHOD FROM 'GET' TO DELETE, SO WE DON'T GET AN ERROR-->
		        <% else %>
		        	<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
					    <%= f.input :email, required: false, autofocus: true %>
					    <%= f.input :password, required: false %>
					    <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
					  	<%= f.button :submit, t('login') %>
					  	<%= link_to t('signup'), new_user_registration_path %>
					<% end %>
		        <% end %>
		    </ul>
	    </div>
	</div>
</nav>

<header></header>